const getNotes = (characterId) => {
    try {
      

    } catch (error) {
        console.log(error);
      
    }
}

const updateNotes = (characterId, note) => {
    try {
       

    } catch (error) {
        console.log(error);

    }
}

export {
    getNotes,
    updateNotes
}