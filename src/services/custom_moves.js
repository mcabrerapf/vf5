const getCustomMoves = (characterId) => {
    try {
      

    } catch (error) {
        console.log(error);
      
    }
}

const updateCustomMoves = (characterId, move) => {
    try {
       

    } catch (error) {
        console.log(error);

    }
}

export {
    getCustomMoves,
    updateCustomMoves
}